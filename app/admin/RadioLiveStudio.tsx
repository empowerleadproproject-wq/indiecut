'use client';import {useEffect,useRef,useState} from 'react';
export default function RadioLiveStudio(){const [sessions,setSessions]=useState<any[]>([]),[title,setTitle]=useState(''),[host,setHost]=useState(''),[guest,setGuest]=useState(''),[msg,setMsg]=useState('');const mic=useRef<MediaStream|null>(null);const load=()=>fetch('/api/admin/radio-live',{cache:'no-store'}).then(r=>r.json()).then(j=>setSessions(j.sessions||[]));useEffect(()=>{load();const i=setInterval(load,5000);return()=>{clearInterval(i);mic.current?.getTracks().forEach(t=>t.stop())}},[]);
async function act(action:string,extra:any={}){setMsg('Working…');const r=await fetch('/api/admin/radio-live',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action,...extra})});const j=await r.json();setMsg(r.ok?(action==='end'?'Live ended. Music automation is ready to resume.':'Saved.'):j.error||'Failed');if(r.ok)load();return j}
async function start(id:string){try{mic.current=await navigator.mediaDevices.getUserMedia({audio:true});await act('start',{id})}catch{setMsg('Microphone permission is required to start the studio.')}}
const active=sessions.find(s=>s.status!=='ended');return <section className="panel" style={{padding:24,marginTop:18}}><div className="kicker">LIVE STUDIO</div><h2>Interviews & Live Shows</h2><p>Create a private guest link, admit guests, and control the live session. Broadcast handoff activates when the external Live365 connection is configured.</p>{msg&&<div className="ic-message">{msg}</div>}{!active?<div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:10}}><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Interview / show title"/><input value={host} onChange={e=>setHost(e.target.value)} placeholder="Host name"/><input value={guest} onChange={e=>setGuest(e.target.value)} placeholder="Expected guest"/><button onClick={()=>act('create',{title,host_name:host,guest_name:guest,record_enabled:true})}>CREATE LIVE STUDIO</button></div>:<div style={{border:'1px solid #ddd',padding:18}}><h3>{active.title}</h3><div><b>Status:</b> {active.status.toUpperCase()}</div><div style={{margin:'10px 0'}}><b>Guest invitation:</b> <input readOnly value={typeof window==='undefined'?'':window.location.origin+'/radio/studio/'+active.guest_token} style={{width:'100%'}}/></div><div style={{display:'flex',gap:8,flexWrap:'wrap'}}>{active.status==='waiting'&&<button onClick={()=>start(active.id)}>START LIVE</button>}{active.status==='live'&&<button onClick={()=>act('end',{id:active.id})}>END LIVE + RESUME MUSIC</button>}</div><h4>Guest Waiting Room</h4>{!active.radio_live_guests?.length?<p>No guest connected yet.</p>:active.radio_live_guests.map((g:any)=><div key={g.id} style={{display:'flex',justifyContent:'space-between',padding:'8px 0',borderTop:'1px solid #eee'}}><span>{g.name} · {g.status}</span>{g.status==='waiting'&&<button onClick={()=>act('guest',{guest_id:g.id,status:'admitted'})}>ADMIT</button>}</div>)}</div>}</section>}