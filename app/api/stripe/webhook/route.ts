import {NextResponse} from 'next/server';import {createClient as service} from '@supabase/supabase-js';import {verifyStripeWebhook} from '../../../../lib/stripe-connect';
export async function POST(req:Request){const raw=await req.text();try{verifyStripeWebhook(raw,req.headers.get('stripe-signature')||'');const event=JSON.parse(raw);const db=service(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!);if(event.type==='account.updated'){const a=event.data.object;await db.from('cut_host_payment_accounts').update({charges_enabled:!!a.charges_enabled,payouts_enabled:!!a.payouts_enabled,details_submitted:!!a.details_submitted,updated_at:new Date().toISOString()}).eq('stripe_account_id',a.id)}if(event.type==='checkout.session.completed'||event.type==='checkout.session.async_payment_succeeded'){const s=event.data.object;const shipping=s.collected_information?.shipping_details||s.shipping_details;await db.from('cut_premium_orders').update({stripe_payment_intent_id:s.payment_intent||null,payment_status:'paid',access_granted:true,customer_email:s.customer_details?.email||null,shipping_name:shipping?.name||null,shipping_address:shipping?.address||null,updated_at:new Date().toISOString()}).eq('stripe_checkout_session_id',s.id)}if(event.type==='checkout.session.async_payment_failed'){const s=event.data.object;await db.from('cut_premium_orders').update({payment_status:'failed',access_granted:false,updated_at:new Date().toISOString()}).eq('stripe_checkout_session_id',s.id)}if(event.type==='charge.refunded'){const c=event.data.object;if(c.payment_intent)await db.from('cut_premium_orders').update({payment_status:'refunded',access_granted:false,updated_at:new Date().toISOString()}).eq('stripe_payment_intent_id',c.payment_intent)}return NextResponse.json({received:true})}catch(e:any){return NextResponse.json({error:e.message||'Webhook rejected'},{status:400})}}
