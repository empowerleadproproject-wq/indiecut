import {NextResponse} from 'next/server';import {createClient as service} from '@supabase/supabase-js';
export async function GET(){const db=service(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false,autoRefreshToken:false}});const{data}=await db.from('site_settings').select('setting_value').eq('setting_key','admin_distribution_page').maybeSingle();let content={};try{content=JSON.parse(data?.setting_value||'{}')}catch{}return NextResponse.json({content},{headers:{'Cache-Control':'no-store, no-cache, must-revalidate'}})}