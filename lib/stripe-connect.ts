import crypto from 'crypto';
const api='https://api.stripe.com/v1';
export function stripeSecret(){const v=process.env.STRIPE_SECRET_KEY;if(!v)throw new Error('STRIPE_SECRET_KEY is not configured');return v}
export async function stripePost(path:string,params:URLSearchParams,account?:string){const r=await fetch(api+path,{method:'POST',headers:{Authorization:'Bearer '+stripeSecret(),'Content-Type':'application/x-www-form-urlencoded',...(account?{'Stripe-Account':account}:{})},body:params.toString(),cache:'no-store'});const j=await r.json();if(!r.ok)throw new Error(j?.error?.message||'Stripe request failed');return j}
export function verifyStripeWebhook(raw:string,sig:string){const secret=process.env.STRIPE_WEBHOOK_SECRET;if(!secret)throw new Error('STRIPE_WEBHOOK_SECRET is not configured');const parts=Object.fromEntries(sig.split(',').map(x=>x.split('=')));const t=parts.t,v1=parts.v1;if(!t||!v1)throw new Error('Invalid Stripe signature');if(Math.abs(Date.now()/1000-Number(t))>300)throw new Error('Expired Stripe signature');const expected=crypto.createHmac('sha256',secret).update(t+'.'+raw).digest('hex');if(expected.length!==v1.length||!crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(v1)))throw new Error('Invalid Stripe signature')}
